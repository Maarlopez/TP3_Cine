function createHeader() {
    const header = document.createElement('header');
    // Configura tu elemento header aquí
    return header;
}

// Exporta esa función para poder usarla en otros archivos
export { createHeader };